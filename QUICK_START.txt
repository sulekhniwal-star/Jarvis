# ⚡ JARVIS Quick Start (30 Seconds)

## 🚀 Get Started in 3 Steps

### Step 1: Install Dependencies (2 minutes)
```bash
cd f:\Jarvis
pip install -r requirements.txt
```

### Step 2: Get API Key (30 seconds)
1. Go to: https://aistudio.google.com/app/apikey
2. Click "Create API Key"
3. Copy the key

### Step 3: Run JARVIS (10 seconds)
```bash
python jarvis.py --api-key "paste-your-key-here"
```

---

## 🎤 Use JARVIS

### Say This ➡️ JARVIS Will Do This
- **"Hey Jarvis"** → Activates and responds "Yes, sir?"
- **"What time is it?"** → Tells current time
- **"Tell me a joke"** → Tells a funny joke
- **"Open Chrome"** → Launches Chrome browser
- **"What's the weather?"** → Shows weather for your city
- **"Set volume to 50"** → Sets system volume
- **"Shutdown"** → Powers off your computer

---

## 🖥️ GUI Mode (Better!)

```bash
python jarvis.py --api-key "your-key" --gui
```

Then click:
- 🎤 **Start Listening** - Begin voice input
- 🔊 **Speak** - Read response aloud
- ❌ **Exit** - Close JARVIS

---

## ⚙️ Customize

Edit `memory.json`:
```json
{
    "owner": "Your Name",
    "city": "Your City"
}
```

---

## 🐛 Troubleshooting

| Issue | Fix |
|-------|-----|
| Microphone not working | Check Windows volume mixer |
| API key error | Go to https://aistudio.google.com/app/apikey |
| GUI doesn't work | Install PyQt5: `pip install PyQt5` |
| No speech recognition | Check internet connection |

---

## 📚 Full Documentation

- **Setup Guide**: See `SETUP.md`
- **Advanced Features**: See `ADVANCED.md`
- **Upgrade Details**: See `UPGRADE_SUMMARY.md`
- **Full README**: See `README.md`

---

## 💡 Pro Tips

1. **Set environment variable** to avoid typing API key every time:
   ```bash
   setx GEMINI_API_KEY "your-key"
   ```

2. **Try different commands** - JARVIS can understand many variations

3. **Use context** - JARVIS remembers previous commands

4. **Extend it** - See `ADVANCED.md` for adding custom features

---

## 🎊 What's Different

✨ **Your old JARVIS:**
- Simple if-else commands
- No memory
- Wake word was just a substring check

✨ **New JARVIS:**
- **AI-powered intent detection**
- **Persistent memory with learning**
- **Hands-free wake word activation**
- **Modern GUI with visualization**
- **Context-aware responses**
- **Extensible architecture**

---

## 🚀 Start Now!

```bash
python jarvis.py --api-key "your-key"
# Say "Hey Jarvis" and start commanding!
```

---

**Questions?** Check the documentation files or read the code comments!
